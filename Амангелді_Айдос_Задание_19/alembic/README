# Alembic Migrations

## Commands

### Create a new migration && apply migrations to database && rollback last migration
```bash
alembic revision --autogenerate -m "migration description"
alembic upgrade head
alembic downgrade -1